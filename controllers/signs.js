const Astro = require("../models/astro");
const LunarCalendar = require('lunar-calendar');


module.exports = {
index,
};


function index(req, res) {
    function index(req, res) {
        res.render("signs/index");
    }
}